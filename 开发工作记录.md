# LeihuoDemo 开发工作记录

## 一、玩家系统

### 动画与移动
- **移动速度调节**：玩家移动速度过快导致与动画步幅不一致，已下调匹配
- **拍照动画重构**：
  - 拍照动画加速至 1~1.5s（原 4s）
  - 取消循环播放
  - 拿起相机后进入静止状态
  - 取消拍照时倒放动画回到待机
  - 拍照期间禁止移动
- **死亡动画**：发现死亡动画中人物高度与其他状态不一致，标记待处理

### 输入修复
- 修复走路时鼠标左键按下导致无法移动的问题

## 二、对话与 UI 系统

### 对话框体系
- **Talk / SpecialTalk / SpecialTalk1**：Talk 用于 NormalWorld，SpecialTalk 用于 SpecialWorld；最终用 SpecialTalk1 取代原 SpecialTalk
- **初始状态**：三个对话框面板初始均设为不激活（m_IsActive=0），避免开场自动弹出

### 物体名称显示
- 新建 `FurnitureDisplayName.cs` 静态字典（19 条），将 pairId/物体名映射为中文名（打印机、桌子、吊灯等）
- `FurnitureInteraction2D.PlayDialogue` 传递 displayName
- `TalkPanelButtonHandler` 新增三参数 Play 重载，驱动 `UIController/Talk/Talk/name/Text` 显示被点击物体名称
- 补充 `lamp`→吊灯映射（场景中物体名为 Lamp 而非 HangingLamp）

### 其他 UI
- **Start Scene**：TutorialLevel 按钮补齐与其余四个按钮一致的位移动画
- **LastRecordPanel/result**：死亡层数播报，汇报玩家死在第几层

## 三、关卡与数据系统

### JSON 关卡配置
- 检查 `Assets/Resources/LevelData/` 下 18 个 JSON（D1~D3 各 6 个），配置正常
- 运行时链路：`RunSaveService` 生成 1-9 层楼层计划 → `LevelTemplateRepository` 加载 JSON → `LevelRuntimeConfigurator.Apply` 匹配场景名并遍历 WorldPairId 配置目标与对话
- **Level_01 目标**（以 D1_BlackPrinterScreen 为例）：NormalWorld 锁定 Printer，SpecialWorld 锁定 Computer
- 楼层映射：1-3 层→Level_01，4-6 层→Level_02，7-9 层→Level_03

### legacy 目标组件
- 确认挂载关系：`NormalWorldVisionTarget2D` 挂 NormalWorld（拍照视界目标），`SpecialWorldInteractionTarget2D` 挂 SpecialWorld（交互恢复目标）
- `LevelRuntimeConfigurator.ApplyWorld` 中 legacy 组件改为 GetComponent+AddComponent，使 JSON 配置可驱动未预挂组件的物体

### 场景文件事故与恢复
- 编辑 Level_02.unity 时文件被截断损坏（921KB→16KB），从备份恢复
- 清理 Unity 为 .bak 文件生成的多余 .meta 文件

## 四、渲染与视觉效果

### 世界转场增强
- `WorldTransitionFX` 新增三层 Overlay：DarkLayer、RedLayer、ScanlineLayer（青色扫描线）
- 镜头脉冲：orthographicSize 缩小 4% 再恢复（0.3s 内）
- 暗红脉冲、延长转场时长（进入 SpecialWorld ~0.9s，返回 NormalWorld 1.6s）、加强抖动
- `WorldSwapManager2D` 进入 SpecialWorld 峰值回调补调 BeginCrossfade

### 美术资源修复
- **SpecialWorld Water1.png**：高度仅为 NormalWorld Water 的一半，裁剪非透明区域后垂直拉伸至 139×480，保留透明通道与画风

### BGM 体系
- `GameAudioManager` 三通道：BGM / Ambient / SFX
- 恐怖气氛为 SpecialWorld 叠加的氛围层，非主 BGM

## 五、背包与道具系统

### 链路打通
- `Drops` 预制体（`DropPickupInteraction2D`）拾取掉落物 → `StoryInventoryManager` 管理 → `PackageCell` 预制体在背包中展示
- 清空背包本地持久化数据，确保运行时从存档重新读取
- 背包结构：运行时清空已有 PackageCell，读取本地数据重建

### 结局三道具
- 四本书/实验记录（id 1~4），对应 `StoryItemPresets.cs`：
  1. 颅骨解剖记录
  2. 肌肉组织标本记录
  3. 胸腔解剖记录
  4. 神经通路实验记录
- 图片路径：`Resources/UI/Items/RecordBook_01~04.png`

### 物品图片抠图修复
- 原始 4 张图存在背景不透明、抠图不干净、光晕等问题
- 方案：image_edit 去除灰绿色背景输出白底 → Python 从边缘 flood fill 纯白背景转透明底 → 清理透明像素 RGB 消除光晕 → 边缘轻微羽化
- 最终 4 张均为干净透明底 PNG，书的细节、血迹、磨损边缘完整保留
- 原始备份：`Resources/UI/Items/backup_before_cleanup/`

## 六、Editor 工具

### 跳关测试工具（完全独立，不修改已有脚本）
- **TestModeWindow.cs**：菜单 `Tools/Leihuo/Test Mode`，开关"第1关后直接进入第9关"，状态存 EditorPrefs
- **TestModeBootstrapper.cs**（Editor-only）：
  - `BeforeSceneLoad`：备份存档，修改 run-save.json（currentFloor=1，第2层 levelSceneName 改为 Level_03），重置 RunSaveService 缓存
  - `sceneLoaded` 事件（注册早于 LevelSceneBootstrapper）：进入 Level_03 且 currentFloor==2 时，反射修改 current.currentFloor=9 并保存，使 LevelRuntimeConfigurator 用第9层配置初始化
  - `Application.quitting`：恢复备份存档
- 效果：第1关通过→加载 Level_03→currentFloor 改为9→第9层配置→第9关通过触发结局二
- `RunSaveService.cs` 已恢复原始状态，无任何修改

## 七、项目研究

- 阅读 Obsidian Vault 中 13 篇技术拆解文档（整体架构、JSON 配置、九层随机计划、存档死亡冻结、双世界配对、视界目标、家具交互、UI 流程、视觉风格、Editor 工具、测试调试、架构演进）
- 梳理全项目场景与脚本关联，记录关键脚本路径与 GUID
- 确认 MCP for Unity 配置位置：`C:\Users\92485\.codex\skills\unity-mcp-skill\`

## 八、备份文件清单

| 文件 | 位置 |
|------|------|
| LevelRuntimeConfigurator.cs | `Assets/Scripts/Level/Data/.bak` |
| RunSaveService.cs | `Assets/Scripts/Save/.bak` |
| WorldSwapManager2D.cs | `Assets/Scripts/Camera/.bak` |
| WorldTransitionFX.cs | `Assets/Scripts/Camera/.bak` |
| TalkPanelButtonHandler.cs | `Assets/Scripts/UI/Dialogue/.bak` |
| Level_02.unity | `Assets/Scenes/.bak` |
| RecordBook_01~04.png | `Assets/Resources/UI/Items/backup_before_cleanup/` |
